<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Header fijo con logo -->
    <header class="fixed-header">
        <div class="container-fluid">
            <div class="row align-items-center">
                <div class="col-auto">
                    <img src="/images/Logo.png" alt="Logo" class="header-logo">
                </div>
                <div class="col">
                    <h1 class="header-title mb-0 text-center">Sistema de Sorteo</h1>
                </div>
            </div>
        </div>
    </header>

    <div class="container-fluid main-content">

        <!-- Contenido principal -->
        <main class="container my-5">
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="card shadow">
                        <div class="card-body p-4">
                            <h3 class="card-title text-center mb-4">Sube tu ticket</h3>

                            <!-- Cuadro unificado para subida, vista previa y escaneo -->
                            <div id="unifiedContainer" class="unified-main-container mb-4">
                                <!-- Estado inicial: Área de drop -->
                                <div id="dropArea" class="drop-area">
                                    <div class="drop-content">
                                        <div class="drop-icon">
                                            <i class="fas fa-cloud-upload-alt" style="color: var(--brand-blue); font-size: 3rem;"></i>
                                        </div>
                                        <p class="mb-2">Arrastra tu imagen aquí</p>
                                        <p class="text-muted small">o</p>
                                        <button type="button" class="btn btn-outline-primary" onclick="document.getElementById('ticketFile').click()">
                                            <i class="fas fa-folder-open me-2"></i>Seleccionar archivo
                                        </button>
                                    </div>
                                    <div id="fileInfo" class="file-info" style="display: none;">
                                        <div class="file-name"></div>
                                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="removeFile()">
                                            <i class="fas fa-times"></i>
                                        </button>
                                    </div>
                                </div>

                                <!-- Estado con imagen: Vista previa y escaneo -->
                                <div id="imagePreview" class="image-unified-container" style="display: none;">
                                    <div class="unified-frame">
                                        <img id="previewImage" src="" alt="Vista previa" class="img-fluid unified-image">
                                        <!-- Barra de escaneo (inicialmente oculta) -->
                                        <div class="scanner-line" id="scannerLine" style="display: none;"></div>
                                        <!-- Información inferior -->
                                        <div class="unified-info" id="unifiedInfo">
                                            <span class="unified-text" id="unifiedText">Imagen lista para validar</span>
                                            <button type="button" class="btn btn-sm btn-outline-danger ms-2" id="changeBtn" onclick="removeFile()">
                                                <i class="fas fa-times"></i> Cambiar
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Formulario -->
                            <form id="uploadForm" enctype="multipart/form-data">
                                <input type="file" id="ticketFile" name="ticket" accept="image/*" style="display: none;" required>

                                <!-- reCAPTCHA -->
                                <% if (typeof recaptchaSiteKey !== 'undefined' && recaptchaSiteKey) { %>
                                    <div class="recaptcha-container mb-3">
                                        <div class="g-recaptcha" data-sitekey="<%= recaptchaSiteKey %>"></div>
                                    </div>
                                <% } %>

                                <div class="d-grid">
                                    <button type="submit" id="submitBtn" class="btn btn-primary btn-lg" disabled>
                                        <span class="spinner-border spinner-border-sm me-2" role="status" style="display: none;"></span>
                                        Validar Ticket
                                    </button>
                                </div>
                            </form>

                            <!-- Pantalla de procesamiento -->
                            <div id="processingScreen" class="text-center mt-4" style="display: none;">
                                <div class="processing-animation mb-3">
                                    <!-- Contenedor de la imagen con marco y escáner -->
                                    <div class="scanner-container">
                                        <div class="image-frame">
                                            <img id="scannedImage" src="" alt="Ticket" class="img-fluid scanned-ticket">
                                            <div class="scanner-line"></div>
                                        </div>
                                    </div>
                                </div>
                                <h4 id="processingTitle">Procesando tu imagen...</h4>
                                <p id="processingMessage" class="text-muted">Estamos validando tu ticket automáticamente. Esto puede tomar unos segundos.</p>
                                <div class="progress mt-3" style="display: none;">
                                    <div class="progress-bar progress-bar-striped progress-bar-animated bg-primary" role="progressbar" style="width: 100%"></div>
                                </div>

                                <!-- Mensaje de error dentro de la pantalla de procesamiento -->
                                <div id="processingError" class="mt-3" style="display: none;">
                                    <div class="alert alert-danger d-inline-block">
                                        <strong>❌ <span id="errorReason"></span></strong>
                                    </div>
                                    <br>
                                    <button type="button" class="btn btn-outline-primary mt-2" onclick="resetUpload()">
                                        <i class="fas fa-redo me-2" style="color: var(--brand-blue);"></i>Cargar Nuevo Ticket
                                    </button>
                                </div>
                            </div>

                            <!-- Mensajes -->
                            <div id="message" class="alert mt-3" style="display: none;"></div>

                            <!-- Instrucciones -->
                            <div class="mt-4 p-3 bg-light rounded">
                                <h6 style="color: var(--brand-blue);">
                                    <i class="fas fa-info-circle me-2"></i>Instrucciones:
                                </h6>
                                <ul class="mb-0 small">
                                    <li>
                                        <i class="fas fa-camera me-2" style="color: var(--brand-blue);"></i>
                                        Toma una foto clara y nítida de tu ticket
                                    </li>
                                    <li>
                                        <i class="fas fa-eye me-2" style="color: var(--brand-blue);"></i>
                                        Asegúrate de que todos los datos sean legibles
                                    </li>
                                    <li>
                                        <i class="fas fa-file-image me-2" style="color: var(--brand-blue);"></i>
                                        Formato permitido: JPG, PNG (máx. 5MB)
                                    </li>
                                    <li>
                                        <i class="fas fa-clock me-2" style="color: var(--brand-blue);"></i>
                                        La validación es automática y toma solo segundos
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <!-- Navegación -->
                    <div class="text-center mt-3">
                        <a href="/" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2" style="color: var(--brand-blue);"></i>Volver al inicio
                        </a>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.12.2/lottie.min.js"></script>
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    <script src="/js/upload.js"></script>
</body>
</html>
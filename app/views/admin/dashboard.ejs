<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin/dashboard">📊 Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/admin/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/participants">Participantes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/configuracion">Configuración</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= adminUsername || 'Admin' %>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/admin/dashboard">Dashboard</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form method="POST" action="/admin/logout" class="d-inline">
                                    <button type="submit" class="dropdown-item">Cerrar Sesión</button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid mt-4">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">📈 Estadísticas</h5>
                    </div>
                    <div class="card-body">
                        <div class="metric-item">
                            <div class="metric-value" id="totalParticipants"><%= stats.total_participants %></div>
                            <div class="metric-label">Total Participantes</div>
                        </div>
                        <div class="metric-item">
                            <div class="metric-value text-success" id="validatedTickets"><%= stats.validated_tickets %></div>
                            <div class="metric-label">Tickets Validados</div>
                        </div>
                        <div class="metric-item">
                            <div class="metric-value text-danger" id="rejectedTickets"><%= stats.rejected_tickets %></div>
                            <div class="metric-label">Tickets Rechazados</div>
                        </div>
                        <div class="metric-item">
                            <div class="metric-value text-info" id="provinces"><%= stats.provinces_count %></div>
                            <div class="metric-label">Provincias</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-9">
                <!-- Gráfico de Participantes Diarios -->
                <div class="row mb-4">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header d-flex justify-content-between align-items-center">
                                <h5 class="mb-0">📊 Participantes Diarios</h5>
                                <div class="btn-group" role="group">
                                    <button type="button" class="btn btn-outline-primary btn-sm active" onclick="changeDays(7)">7 días</button>
                                    <button type="button" class="btn btn-outline-primary btn-sm" onclick="changeDays(15)">15 días</button>
                                    <button type="button" class="btn btn-outline-primary btn-sm" onclick="changeDays(30)">30 días</button>
                                </div>
                            </div>
                            <div class="card-body">
                                <div id="chart-container" style="width: 100%; height: 400px; background: #f8f9fa; border-radius: 8px; display: flex; align-items: center; justify-content: center;">
                                    <div style="text-align: center; color: #666;">
                                        <div style="font-size: 48px; margin-bottom: 10px;">📊</div>
                                        <div>Cargando gráfico...</div>
                                    </div>
                                </div>
                                <div class="mt-3 text-center">
                                    <small class="text-muted">
                                        Gráfico de participantes registrados por día
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                
                                <!-- Error message -->
                                <% if (typeof error !== 'undefined' && error) { %>
                                    <div class="alert alert-danger mt-3">
                                        <%= error %>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script>
        // Cargar MUI X-Charts dinámicamente
        const script = document.createElement('script');
        script.src = 'https://unpkg.com/@mui/x-charts@7.0.0/umd/index.js';
        script.onload = function() {
            console.log('MUI X-Charts cargado correctamente');
            window.muiChartsLoaded = true;
        };
        script.onerror = function() {
            console.error('Error cargando MUI X-Charts');
        };
        document.head.appendChild(script);
    </script>
    <script src="/js/admin-dashboard.js"></script>


    <script>
        // Datos del gráfico
        const dailyMetrics = <%- JSON.stringify(dailyMetrics || []) %>;
        let currentDays = 7;

        console.log('Datos del gráfico:', dailyMetrics);

        // Función para preparar datos del gráfico
        function prepareChartData(data, days) {
            if (!data || data.length === 0) {
                // Crear datos vacíos para los últimos días
                const emptyData = [];
                for (let i = days - 1; i >= 0; i--) {
                    const date = new Date();
                    date.setDate(date.getDate() - i);
                    emptyData.push({
                        date: date.toISOString().split('T')[0],
                        participants_count: 0
                    });
                }
                return emptyData;
            }

            return data;
        }

        // Función para formatear valores
        function valueFormatter(value) {
            return `${value} participantes`;
        }

        // Componente React para el gráfico simplificado
        function DailyParticipantsChart({ data, days }) {
            const chartData = prepareChartData(data, days);
            console.log('Renderizando gráfico con:', chartData);

            // Si no hay datos, mostrar mensaje
            if (!chartData || chartData.length === 0) {
                return React.createElement('div', {
                    style: {
                        width: '100%',
                        height: '350px',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        backgroundColor: '#f8f9fa',
                        borderRadius: '8px',
                        color: '#666'
                    }
                },
                    React.createElement('div', { style: { textAlign: 'center' } },
                        React.createElement('div', { style: { fontSize: '48px', marginBottom: '10px' } }, '📊'),
                        React.createElement('div', null, 'No hay datos disponibles')
                    )
                );
            }

            return React.createElement('div', { style: { width: '100%' } },
                React.createElement('h5', { style: { marginBottom: '20px', color: '#333' } }, 'Participantes Diarios Registrados'),
                React.createElement(mui.xCharts.BarChart, {
                    dataset: chartData,
                    series: [{
                        dataKey: 'participants_count',
                        label: 'Participantes',
                        color: '#007bff'
                    }],
                    xAxis: [{
                        dataKey: 'date',
                        scaleType: 'band',
                        valueFormatter: (value) => {
                            const date = new Date(value + 'T00:00:00');
                            return date.toLocaleDateString('es-ES', {
                                day: '2-digit',
                                month: '2-digit'
                            });
                        }
                    }],
                    height: 350,
                    margin: { left: 60, right: 30, top: 20, bottom: 40 },
                    yAxis: [{
                        width: 60,
                        valueFormatter: valueFormatter
                    }],
                    grid: { vertical: true, horizontal: true },
                    tooltip: {
                        trigger: 'axis',
                        valueFormatter: valueFormatter
                    }
                })
            );
        }

        // Componente principal de la app
        function App() {
            const [chartData, setChartData] = React.useState(dailyMetrics);
            const [days, setDays] = React.useState(currentDays);

            return React.createElement(DailyParticipantsChart, {
                data: chartData,
                days: days
            });
        }

        // Renderizar la app React después de que MUI se cargue
        function initializeChart() {
            console.log('DOM cargado, inicializando gráfico...');
            const container = document.getElementById('chart-container');
            if (container) {
                console.log('Contenedor encontrado, verificando MUI...');
                if (typeof mui !== 'undefined' && mui.xCharts) {
                    console.log('MUI disponible, renderizando React...');
                    try {
                        ReactDOM.render(React.createElement(App), container);
                        console.log('React renderizado exitosamente');
                    } catch (error) {
                        console.error('Error renderizando React:', error);
                        container.innerHTML = '<div style="text-align: center; color: red;">Error cargando gráfico</div>';
                    }
                } else {
                    console.log('MUI no disponible aún, esperando...');
                    setTimeout(initializeChart, 100);
                }
            } else {
                console.error('Contenedor del gráfico no encontrado');
            }
        }

        document.addEventListener('DOMContentLoaded', initializeChart);

        // Función global para cambiar días (llamada desde botones HTML)
        window.changeDays = async function(days) {
            currentDays = days;

            // Actualizar botones activos
            document.querySelectorAll('.btn-group .btn').forEach(btn => {
                btn.classList.remove('active');
            });
            event.target.classList.add('active');

            try {
                const response = await fetch(`/admin/api/daily-metrics?days=${days}`);
                const result = await response.json();

                if (result.success) {
                    // Re-renderizar el gráfico con nuevos datos
                    const container = document.getElementById('chart-container');
                    if (container) {
                        ReactDOM.render(
                            React.createElement(DailyParticipantsChart, {
                                data: result.data,
                                days: days
                            }),
                            container
                        );
                    }
                } else {
                    console.error('Error cargando métricas:', result.message);
                }
            } catch (error) {
                console.error('Error:', error);
            }
        };
    </script>
</body>
</html>